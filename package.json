{
  "name": "prompt-shelf",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension for browsing local agent prompts and copying agent-specific sections.",
  "scripts": {
    "test": "node --test test/*.test.cjs"
  }
}

